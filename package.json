{
  "name": "gh-plus",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsc && vite build --watch",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "typescript": "~5.9.3",
    "vite": "^8.0.0"
  }
}